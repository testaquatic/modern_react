import type { ReactElement } from "react";

export default function LazyButton(): ReactElement {
    return (
        <form action="#" method="POST">
            <button type="button">버튼1</button>
        </form>
    );
}
